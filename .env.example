# =============================================================================
# Marketing Connect MCP Server Configuration
# =============================================================================
# Copy to .env and customize for your setup
# All variables prefixed with MCP_
#
# HOW THIS WORKS:
# 1. The server reads environment variables on startup
# 2. Variables starting with MCP_ are loaded into settings
# 3. You can set these in your shell, or put them in a .env file
# =============================================================================

# Server Identity (shows in logs and MCP client discovery)
MCP_SERVER_NAME=marketing-connect-mcp-services
MCP_SERVER_VERSION=1.0.0

# HTTP Server Configuration
# HOST: Which network interface to bind to
#   - "0.0.0.0" = Listen on ALL interfaces (accessible from other machines)
#   - "127.0.0.1" = Localhost only (only accessible from this machine)
MCP_HOST=0.0.0.0
MCP_PORT=8000

# Logging and Debug
# DEBUG: When true, prints extra info like registered tools at startup
# LOG_LEVEL: Controls how verbose the logs are
#   - DEBUG = Everything (very verbose)
#   - INFO = Normal operation
#   - WARNING = Only problems
#   - ERROR = Only errors
MCP_DEBUG=false
MCP_LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------

# Base URL for the application
MCP_BASE_URL=

# Deployment region
MCP_REGION=

# -----------------------------------------------------------------------------
# ADD YOUR SETTINGS HERE
# -----------------------------------------------------------------------------
# When you add settings to config.py, document them here too!
#
# Example: External API
# MCP_API_BASE_URL=https://api.example.com
# MCP_API_KEY=your-key-here
#
# Example: Database
# MCP_DATABASE_URL=postgresql://user:pass@localhost/db
